apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cfs-security-kbs-pvc
  namespace: confidential-filesystems
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-rbd
  volumeMode: Block